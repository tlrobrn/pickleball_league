<h2>New group</h2>

<%= render "form.html", changeset: @changeset,
                        players: @players,
                        action: group_path(@conn, :create) %>

<%= link "Back", to: group_path(@conn, :index) %>
