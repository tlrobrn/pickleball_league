<h2>Listing players</h2>

<table class="table">
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>EPR</th>
      <th>Opponent EPR</th>
      <th>Games Played</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
    <%= for {player, epr} <- sorted_players_with_eprs(@players, @opponent_eprs) do %>
    <tr>
      <td><%= player.first_name %></td>
      <td><%= player.last_name %></td>
      <td><%= format_epr(epr) %></td>
      <td><%= format_epr(@opponent_eprs[player.id]) %></td>
      <td><%= length(player.earned_points_ratios) %></td>

      <td class="text-right">
        <%= link "Show", to: player_path(@conn, :show, player), class: "btn btn-default btn-xs" %>
        <%= link "Edit", to: player_path(@conn, :edit, player), class: "btn btn-default btn-xs" %>
        <%= link "Delete", to: player_path(@conn, :delete, player), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link "New player", to: player_path(@conn, :new) %>
